services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5433:5432"
    env_file:
      - ./apps/api/.env
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: